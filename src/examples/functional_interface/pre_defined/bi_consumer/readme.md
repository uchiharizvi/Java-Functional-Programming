# Bi Consumer<T,U>
**Description**: Represents an operation that accepts two input arguments and returns no result.

**Method**: void accept(T t, U u)

**Example**: Used for performing actions that consume two inputs.